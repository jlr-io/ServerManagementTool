<%= form_with(model: ticket, local: true) do |form| %>
  <% if ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
      <% ticket.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <% if !current_user.admin %>
    <div class="field">
      <%= form.label :user_id %>
      <%= form.text_field :user_id, value: @current_user.id, :class => 'text_field', :disabled => true %>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :server_id %>
    <%= form.number_field :server_id %>
  </div>

  <div class="field">
    <%= form.radio_button :ticket_type, 'edit' %>
    <%= label :ticket_type, 'Edit', :value => 'edit' %>
    <%= form.radio_button :ticket_type, 'delete' %>
    <%= label :ticket_type, 'Delete', :value => 'delete' %>
  </div>

  <div class="field">
    <%= form.label :comments %>
    <%= form.text_area :comments %>
  </div>

<% if current_user.admin %>
  <div class="field">
    <%= form.label :accepted %>
    <%= form.check_box :accepted, :class => "formcheckbox" %>
  </div>
  
  <div class="field">
    <%= form.label :complete %>
    <%= form.check_box :complete, :class => "formcheckbox" %>
  </div>
<% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
